jar {
    manifest {
        attributes(
                'Main-Class': 'com.ntask.task2.ApplicationMain'
        )
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}