jar {
    manifest {
        attributes(
                'Main-Class': 'com.ntask.task1.ApplicationMain'
        )
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
